# Настройка Telegram интеграции

## Обзор

FlowOne поддерживает интеграцию с Telegram для отправки уведомлений пользователям о важных событиях в системе документооборота.

## Требования

1. **Telegram Bot Token** - получите токен от [@BotFather](https://t.me/botfather)
2. **PostgreSQL база данных** - для хранения привязок пользователей

## Настройка

### 1. Создание Telegram бота

1. Откройте Telegram и найдите [@BotFather](https://t.me/botfather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Сохраните полученный токен

### 2. Настройка переменных окружения

Добавьте в файл `.env`:

```env
TELEGRAM_BOT_TOKEN=your_bot_token_here
TELEGRAM_BOT_USERNAME=your_bot_username
```

### 3. Применение миграции базы данных

```bash
npx prisma migrate dev
```

Это добавит следующие поля в таблицу `User`:
- `telegramChatId` - ID чата пользователя в Telegram
- `telegramUsername` - Username пользователя
- `telegramFirstName` - Имя пользователя
- `telegramLastName` - Фамилия пользователя
- `telegramBindingCode` - Временный код для привязки
- `telegramBindingExpiry` - Время истечения кода

### 4. Настройка Telegram бота (webhook)

Создайте файл для обработки webhook от Telegram:

```typescript
// app/api/webhooks/telegram/route.ts
import { NextRequest, NextResponse } from "next/server";
import { prisma } from "@/lib/prisma";

export async function POST(request: NextRequest) {
  const body = await request.json();
  
  // Обработка команды /bind
  if (body.message?.text?.startsWith("/bind ")) {
    const code = body.message.text.split(" ")[1];
    const chatId = body.message.chat.id;
    const user = body.message.from;
    
    // Найти пользователя с этим кодом
    const dbUser = await prisma.user.findFirst({
      where: {
        telegramBindingCode: code,
        telegramBindingExpiry: {
          gte: new Date(),
        },
      },
    });
    
    if (dbUser) {
      // Привязать аккаунт
      await prisma.user.update({
        where: { id: dbUser.id },
        data: {
          telegramChatId: chatId.toString(),
          telegramUsername: user.username,
          telegramFirstName: user.first_name,
          telegramLastName: user.last_name,
          telegramBindingCode: null,
          telegramBindingExpiry: null,
        },
      });
      
      // Отправить подтверждение
      await sendTelegramMessage(chatId, "✅ Аккаунт успешно привязан!");
    } else {
      await sendTelegramMessage(chatId, "❌ Неверный или истёкший код");
    }
  }
  
  return NextResponse.json({ ok: true });
}

async function sendTelegramMessage(chatId: number, text: string) {
  const token = process.env.TELEGRAM_BOT_TOKEN;
  await fetch(`https://api.telegram.org/bot${token}/sendMessage`, {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify({ chat_id: chatId, text }),
  });
}
```

### 5. Установка webhook

```bash
curl -X POST "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/setWebhook" \
  -H "Content-Type: application/json" \
  -d '{"url": "https://your-domain.com/api/webhooks/telegram"}'
```

## Использование

### Привязка аккаунта

1. Пользователь переходит на страницу `/telegram`
2. Нажимает кнопку "Привязать Telegram"
3. Получает код привязки (действителен 10 минут)
4. Открывает бота в Telegram
5. Отправляет команду `/bind КОД`
6. Получает подтверждение

### Отправка уведомлений

```typescript
import { sendTelegramNotification } from "@/lib/telegram";

// Отправить уведомление пользователю
await sendTelegramNotification(userId, {
  title: "Новый документ",
  message: "Вам назначен документ на согласование",
  documentId: 123,
});
```

## Типы уведомлений

- 📄 **Новый документ** - когда пользователю назначен документ
- ✅ **Согласование** - когда документ согласован
- ❌ **Отклонение** - когда документ отклонён
- 💬 **Комментарий** - когда добавлен комментарий
- ⏰ **Напоминание** - о приближении дедлайна

## Безопасность

- Коды привязки действительны только 10 минут
- Каждый код можно использовать только один раз
- Пользователь может отвязать аккаунт в любой момент
- Все данные хранятся в зашифрованном виде

## Troubleshooting

### Бот не отвечает

1. Проверьте, что токен правильный
2. Убедитесь, что webhook установлен
3. Проверьте логи сервера

### Код не работает

1. Проверьте, что код не истёк (10 минут)
2. Убедитесь, что код введён правильно
3. Попробуйте сгенерировать новый код

### Уведомления не приходят

1. Проверьте, что аккаунт привязан
2. Убедитесь, что бот не заблокирован пользователем
3. Проверьте настройки уведомлений
